<div class="page-header">
  <h1>Rock Paper Scissors</h1>
</div>
<%= form_tag throw_option_path ,:id => 'game_select' , :method => 'post'  do %>
<div>	
	<%= select("select", "select_id", @selections.each_with_index.map {|name, index| [name,index]}, {include_blank: false})		%>		   
</div>
<div>	
	<%= submit_tag "Roshambo", :name => nil , :class => 'btn  btn-success' %>
</div>
<% end %>
 
 
